***********************************************
Phase 0: Set up Environment + Clone CT-RATE
***********************************************

1. Initialize repo

-> uv init

2. Clone the CT-CLIP repo 

-> git clone https://github.com/ibrahimethemhamamci/CT-CLIP.git external/ct-clip

cd external/ct-clip/transformer_maskgit
uv pip install -e .
cd ../CT_CLIP
uv pip install -e .
cd ~/projects/Chest-CT-ZeroShot-Benchmark


***************************************************************
Phase 1: Get CT-CRATE + Checkpoint Access
***************************************************************

1. Create a Hugging Face account, accept the CT-RATE gate on the dataset page, generate an access token (Settings → Access Tokens).

-> save token as HF_TOKEN=<your_access_token> in .env 

2. Download the small CSVs first (cheap, not the volumes): validation_metadata.csv, validation_reports.csv, valid_labels.csv (or the multi_abnormality_labels/valid_predicted_labels.csv equivalent).

-> uv pip install huggingface_hub
-> uv run python EDA/get_csvs.py 

3. Take a random subset of ~100-150 VolumeNames from valid_labels.csv.

-> uv run python EDA/get_subset.py 

4. Adapt the download script: fix the folder path to dataset/valid_fixed/, use your subset list instead of the full file, pull your HF token from an environment variable instead of hardcoding it.

-> no longer required as this is already taken care of in the new labels 
-> use HfApi().list_repo_files() to get the dataset's actual file list from HF directly, then filter — instead of manually rebuilding paths from VolumeName strings and hoping the convention matches
-> taking first 100 samples

5. Sanity check one volume before downloading the rest: load it, check whether raw pixel values already look like real HU (air ≈ -1000) before applying the script's slope/intercept correction. If they already do, we skip that step in preprocessing; if not, we keep it as written.

-> uv run python EDA/sanity_check.py 
-> these are already rescaled values as per the documentation so no need to preprocess 


***************************************************************
Phase 2: CT-CLIP + BiomedCLIP 
***************************************************************

1. Download the checkpoint. 
--> Same hf_hub_download pattern as the CSVs, pointed at models/CT-CLIP-Related/CT-CLIP_v2.pt (a few GB, so this'll take a bit).

-> uv run python src/chest_ct_zeroshot_benchmark/get_checkpoint.py 

2. Fork the dataset class with the intensity fix. 
--> Copy CTReportDatasetinfer out of data_inference_nii.py into your own repo, 
--> and in nii_img_to_tensor, delete the line img_data = slope * img_data + intercept — the volumes are already HU-corrected, so we go straight from the loaded array to the HU clip step. 
--> Keep the resample-to-target-spacing, /1000 normalization, and crop/pad logic exactly as written.

-> uv run python src/chest_ct_zeroshot_benchmark/dataset_fixed.py 

3. Run CT-CLIP zero-shot inference. 
--> Use run_zero_shot.py as the template, pointed at your local checkpoint, your data_volumes folder, and the three CSVs — but swap in your fixed dataset class. 
--> Save per-volume, per-finding probability scores to disk (a CSV or dataframe) rather than just letting it print results, so you have raw scores for both models to compare later.

-> uv pip install transformers accelerate nibabel scikit-learn pandas numpy torch
-> uv run python src/chest_ct_zeroshot_benchmark/ctclip_zeroshot.py 

-> create s3 bucket - upload data_volumes, checkpoints, raw_csvs
-> create ec2 instance and ssh in 
-> 


4. Build the BiomedCLIP baseline on the same preprocessed volumes. 
--> Reuse the same fixed preprocessing function up through the point where you have a clean 3D HU tensor, extract axial slices from it, embed each slice with BiomedCLIP's image tower, mean-pool to a volume-level embedding, 
--> then score against the identical "{finding} is present." / "{finding} is not present." prompt pairs with softmax — same scoring logic as CT-CLIP, just swapping the vision backbone. 
--> This keeps the comparison fair since both models see the same preprocessed input and the same prompt phrasing.

5. Compute metrics for both, side by side. 
--> Per-finding AUC-ROC and F1 via sklearn (mirroring eval.py's approach), across your 100-volume subset, for CT-CLIP and BiomedCLIP separately. 
--> This table is our actual result.

***************************************************************
Phase 3: Documentation 
***************************************************************